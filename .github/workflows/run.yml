on: push 

jobs:
  build:
    runs-on: ubuntu-latest
    name: run R script 
    steps:
      - uses: actions/checkout@v3
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.2.2'
      - run: Rscript -e 'print("hello")'